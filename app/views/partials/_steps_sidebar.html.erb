<% steps.each do |step| %>
  <%= render(
    partial: 'partials/step_label',
    locals: {
      number: step[:number],
      title: step[:title],
      is_current_step: step[:is_current_step]
    }
  ) %>
<% end %>
