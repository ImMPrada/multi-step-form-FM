<!DOCTYPE html>
<html>
  <head>
    <title>MultiStepFormFm</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-dark-white">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <main class="container my-0 w-screen h-screen mx-auto flex flex-col justify-center bg-dark-white">

      <% side_bar = content_tag(
          :div,
          render(
            partial: 'partials/steps/sidebar',
            locals: { steps: @steps }
          ),
          class: '
            flex
            flex-col
            gap-8
            sidebar
            min-h-full
            rounded-xl
            w-68.5
            h-full
            py-10
            px-8
          '
        )
      %>

      <% form_div = content_tag(
          :div,
          yield,
          class: 'min-h-full w-112.5 mx-auto py-4 bg-red-50'
        )
      %>

      <%= content_tag(
        :div,
        side_bar + form_div,
        class: 'w-235 h-142 mx-auto rounded-2xl flex gap-5 bg-white p-4'
      )%>

    </main>
  </body>
</html>
